# V7 实施与验证记录

目标：按已批准的多法律导入、默认全库检索和显式 metadata 过滤方案完成 V7。
设计：../specs/2026-09-21-v7-multi-law-design.md

- [ ] 1. 在 `tests/test_legal_corpus.py` 先验证跨页法条、章节、附录排除、完整性检查和同号法条；实现 `legal_corpus.py` 与 splitter 改动。
- [ ] 2. 测试真实临时 Chroma 的幂等导入、指纹变化和失败不发布；实现 `ingest_corpus` / `open_corpus`。测试使用本地确定性 Embedding，不调用远端。
- [ ] 3. 测试原始问题优先、中文/数字条号、多法律比较、未知法律、全库与过滤区别；通过 `resolve_filter` 及 `build_rag_chain` 接入生产检索。
- [ ] 4. 通过 `main.py --ingest` 统一入口，重用工厂构建无重排对照；改进来源显示并回归现有多轮测试。
- [ ] 5. 复用评测器支持 law_id + article，新增 V7 题和过滤开关，保留历史题。检查空预期不被统计为命中。
- [ ] 6. 执行全部本地测试和 diff 检查，真实导入 107 + 98 + 54 条。重跑导入确认无重复，并记录真实全库/过滤检索与多轮回答结果。
- [ ] 7. 复核改动、记录版本、配置、评测摘要，提交 Git；按用户的 main 分支工作习惯同步远程，不创建 PR。
